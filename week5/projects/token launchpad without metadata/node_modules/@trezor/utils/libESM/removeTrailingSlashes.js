export const removeTrailingSlashes = (input) => {
    if (!input) {
        return input || '';
    }
    let i = input.length;
    while (i > 0 && input[i - 1] === '/') {
        i--;
    }
    return input.substring(0, i);
};
//# sourceMappingURL=removeTrailingSlashes.js.map